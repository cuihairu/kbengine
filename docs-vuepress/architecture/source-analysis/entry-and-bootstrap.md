# 启动入口与引导流程

> 本页用于回答：一个 KBEngine 组件进程从可执行文件启动后，代码第一步是怎么走的？

## 建议覆盖的主题

- 各服务端组件的启动入口
- 统一启动框架与组件差异
- 配置加载与环境初始化
- 网络、脚本、日志、Watcher 等子系统的初始化顺序
- 进入主循环前完成了哪些注册动作

## 建议重点跟踪

- 可执行入口文件
- `ServerApp` / `EntityApp` 一类的公共基类
- 组件注册与工厂逻辑
- 主循环建立位置
