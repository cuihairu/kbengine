# 脚本运行时与热重载

> 本页关注 Python 脚本层如何嵌入 KBEngine 运行时。

## 建议覆盖的主题

- 脚本模块初始化
- C++ 到 Python 的绑定方式
- Python Entity 类的创建时机
- 回调、属性访问、方法暴露的桥接逻辑
- `reloadScript` 一类热重载机制的实现边界

## 建议重点跟踪

- Python 嵌入初始化代码
- 实体脚本实例化过程
- 脚本异常与日志处理路径
